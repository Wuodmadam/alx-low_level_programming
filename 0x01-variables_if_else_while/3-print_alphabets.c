#include <stdio.h>
/**
 * main - Printing the alphabet in lowercase followed by uppercase.
(*
 * Return: 0 on success
 */
int main(void)
{
	char q = 'a';
	char v = 'A';

	while (q <= 'z')
	{
		putchar(c);
		q++;
	}
	while (v <= 'Z')
	{
		putchar(d);
		v++;
	}
	putchar('\n');
	return (0);

}
